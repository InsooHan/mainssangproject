<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fpro.data.mapper.StoreMapperInter">
	<select id="getTotalCount" resultType="int">
		select count(*) from store
	</select>
	
	<insert id="insertStore" parameterType="store">
		insert into store (store_product, store_photo, store_price, store_category) values(#{store_product},#{store_photo},#{store_price},${store_category})
	</insert>
	
	<select id="getAllStore" resultType="store">
		select * from store order by num desc
	</select>
</mapper>