<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fpro.data.mapper.DemovieMapperInter">
	<select id="getSomeDatas" parameterType="Map" resultType="demovie">
		select d.movietime, s.capacity from demovie d, sang s where d.movie_num=#{moviename} and d.theater_num=#{theater} and d.sang_num=s.num;
	</select>
</mapper>